# 电磁炮工作站失效提示文案设计

## 目标

将电磁炮无法解析已绑定设备时显示的含糊提示“无线接入点离线或被破坏”，改为“找不到绑定的过载设备工作站”，让玩家明确需要检查电磁炮所绑定的过载设备工作站。

## 变更范围

- 仅修改翻译键 `ae2lt.railgun.fail.no_ap`。
- 中文文案改为“找不到绑定的过载设备工作站”。
- 英文文案同步表达相同含义。
- 不修改其他“无线接入点”“无线网络”相关文案。
- 不修改触发条件、绑定逻辑或失败码。

## 验证

按用户要求不新增自动化测试。修改后检查中英文 JSON 语法，并确认该翻译键仍各自只定义一次。
