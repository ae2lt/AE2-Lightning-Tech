# 苍穹拟态手杖

在独立工作区的 `feat/celestweave-mimicry-staff` 分支实现，起点为本地 `main` 的 `ba7319b2`。物品 ID 为 `ae2lt:celestweave_mimicry_staff`。

## 使用

- 主手或副手持有手杖，按原有装备配置快捷键（默认 **G**），在原有过载设备配置界面选择手杖页。潜行右键空气也打开同一个界面。
- 模块通过原有**过载设备工作台**装卸，沿用 20 tick 安装进度、输入扣除和拆卸返回流程。手杖不需要额外结构核心。
- 手杖使用 FE 充电，合成后初始无电，容量 **100,000,000 FE**。创造栏提供满电样品。
- 镐、剑、斧同时可用，不切换工具类型。基础挖掘效能 8，基础总攻击伤害 7，攻击速度 1.6。

## 模块

共有十二个用途槽：原有十个手杖模块槽，加过载内核槽与处决槽。新增十一个手杖模块物品，并复用三个现有电磁炮模块；每类最多一个，两个等级模块共用等级槽，两个处决模块共用处决槽。升级等级前须先取出旧模块。

| 模块 | 规则 |
| --- | --- |
| 鹤嘴锄 | 铲和锄同时可用；土地操作单独选择关闭、耕地、铲路 |
| 剪切 | 剪刀适用方块、原生可剪切实体、南瓜、蜂巢 |
| 刀具 | 农夫乐事原生刀具能力与刀具战利品判定 |
| 扳手 | AE2 原生旋转与拆卸；Mek 默认配置物品，独立选择其他资源、旋转、拆卸 |
| 下界合金采集 | 下界合金材质等级，效能 9 |
| 最高采集等级 | 解除普通材质等级门槛，效能仍为 9；保留工具类型要求与方块自身破坏限制，不破坏基岩 |
| 速度调节 | 开关及 `基础时间 + 原始耗时 × 倍率` |
| 伤害增强 | 调节基础攻击伤害和范围，默认 10 伤害、单体；承载战斗附魔 |
| 采集增强 | 普通、时运 III+、精准采集 I；承载工具类附魔 |
| 自动熔炼 | 本次产物按世界当前熔炉配方转换一次，默认关闭 |
| 收集增强 | 本次产物插入背包，余量正常掉落，默认关闭 |

“最高采集等级模块”早期显示名为“无界采集模块”；“收集增强模块”早期显示名为“回背包模块”。本次统一了显示名，内部物品 ID 保持稳定。

## 攻击范围、伤害与处决

在装备配置界面选择伤害增强模块，分别调整攻击范围和伤害：

- 范围为单体、原生横扫、3×3、5×5、7×7。方形范围以主目标命中前的位置为中心，按实体碰撞箱相交判定，垂直范围为下方 1 格至上方 2 格。满蓄力主目标成功命中后才触发，不递归扩散；跳过盟友、自己的宠物、盔甲架和被墙遮挡的附加目标。附加目标仍逐个经过原生攻击事件和附魔处理。
- 基础攻击伤害为 **1、5、10、20、100、500、无限**。这些数值包含玩家基础伤害，附魔、蓄力、暴击、护甲等仍沿原生流程处理。500 需要安装现有**过载内核模块**；无限需要**多维处决模块**。选项跳过尚未解锁档位；卸下解锁模块立即回落到最高可用档位。
- 无限在伤害调用处使用有限的 `Float.MAX_VALUE`，避免无穷属性或 NaN；附魔属性栏仍显示有限值，所选无限档位以配置页为准。关闭处决时仍可被原生伤害保护拒绝。
- 安装现有**过载处决模块**或**多维处决模块**后，在该模块参数中选择关闭、正常死亡、强制移除。需要伤害增强模块与满蓄力近战；默认正常死亡。关闭时仅保留所选原生伤害；开启后复用电磁炮同一套血量记录、衰减、死亡归属与移除实现，遵守处决总开关及玩家目标保护。
- 普通伤害先处理，过载血量记录随后扣减本次攻击伤害，与电磁炮顺序一致。每个接受过载结算的目标额外消耗 **20,000,000 FE**，并预留该次命中的 1,000 FE；不足时只保留可支付的普通攻击。多维处决没有这笔额外费用。
- 方形范围内每个成功命中单独收取 1,000 FE，按距主目标从近到远处理，电量不足即停止。原生横扫沿用原生一次挥击收费和副目标伤害规则，额外副目标不进入手杖处决；需要范围处决时选择方形范围。

## 闪电消耗

日常挖掘、剪切、砧板、扳手，以及 1／5／10／20 伤害的单体近战仍按原 FE 规则运行。强化战斗额外从绑定的 ME 网络提取闪电。把手杖放入联网的过载设备工作台即可自动绑定；工作台所在区块必须已加载，且网络正常供能。绑定沿用电磁炮的距离与维度规则，保存后仍保留。

| 攻击配置 | 每次原生命中的闪电费用（默认） |
| --- | --- |
| 1、5、10、20 伤害，单体 | 无 |
| 100 伤害 | 32 极高压 |
| 500 伤害 | 96 极高压 |
| 无限伤害 | 256 极高压 |
| 满蓄力且启用处决 | 极高压费用至少 256，与伤害档位取较高值，不重复相加 |
| 满蓄力横扫、3×3、5×5、7×7 | 额外 1 高压；方形范围按每个实际命中分别收费，原生横扫按一次挥击收费 |

三级极高压费用直接沿用电磁炮配置 `railgun.energy.ehvCostTier1/2/3`。过载内核启用 **16 高压抵 1 极高压**补偿；先使用极高压，缺额才用高压补足。多维处决仍免除过载处决的 2000 万 FE 附加费，但不免闪电费。

命中前检查并预扣整笔闪电；不足时不产生该次攻击或处决效果，也不扣基础 FE。原生命中与处决均未生效时退回预扣闪电。方形范围按近到远处理，剩余闪电或 FE 不足就停止。全额费用的模拟与实际提取不一致时退回已提取部分。

装备配置界面的伤害增强和处决模块下显示当前满蓄力攻击的高压、极高压费用，只读且随配置变化。状态“战斗资源不足”指当前战斗配置缺少 FE、闪电或有效网络；普通工具操作仍单独检查 FE。

## 手杖相位锁定

装备配置页的参数列表末尾提供“手杖相位锁定”，默认关闭，无需额外模块。开启时，将当前手杖原件移交给服务器私有槽，原位置换成独立物品 ID `ae2lt:mimicry_staff_projection` 的投影。私有槽按玩家和原物品栏位置保存唯一真身，不暴露物品提取接口。

- **不可移动**：投影禁止模拟／实际提取、Q／Ctrl-Q 丢弃、界面取放、快捷移动、数字键交换及 F 键主副手交换。滚动选择不同快捷栏格不移动物品，可正常使用。服务器包处理处同样检查重写 `clicked` 的菜单。
- **使用真身**：手杖能力、模块附魔、FE、闪电网络绑定与处决血量记录都在同一真身上结算。原版挖掘／方块使用入口创建的临时工具副本，只在该次调用内获准读取真身，以保留精准采集等战利品行为；普通 `copy()` 不获得这种权限。
- **投影无载荷**：投影只含所属玩家、真身编号、代次和固定位置，以及外观名称、设置、模块类型位图、电量的展示视图。它不携带真实模块、物品容器、原始附魔、FE 储存组件或任意第三方数据，不向真身回写第三方添加的投影组件。客户端同步不包含真身载荷。
- **防拆卸**：投影不进入过载设备工作台的设备适配器，模块接口拒绝安装和拆卸；它没有原手杖的配方物品 ID。装卸模块、整理位置前先解锁。
- **单次解锁**：先撤销私有条目与所有旧代次投影，再把唯一真身原件放回原格。解锁不要求 FE 或闪电，保留实际剩余电量、模块、附魔与第三方数据。
- **失效与补位**：有效投影必须同时匹配玩家、真身、代次、固定位置与活动堆叠引用。无对应真身、他人或错误位置的投影在正常物品栏写入时变为空；绕开写入直接改列表的失效项对普通读取返回空，并在玩家 tick 清除。投影缺失时生成新代次标记，旧投影立即失效，不复制或恢复工具载荷。原格被普通物品占据时先把它移到空位；背包满或被其他受保护物品占据时等待空间，不覆盖该物品。
- **死亡与存档**：真身一直留在服务器私有槽。重生使旧投影失效并建立新投影；保存的是同一份原件，重新加载后恢复关联。普通物品仍按原死亡规则掉落。

保留此前全部拦截层：NeoForge 玩家主物品栏／副手／装备与 `ItemStackHandler` 的两种提取模式、原版移除、`dropAll`、GUI、创造模式上传、犬牙钓竿候选过滤、机会方块炸弹覆盖及清空奖励的捕获／延迟恢复过滤。`ItemEntity.setItem` 将受保护参数替换为 `ItemStack.EMPTY`，覆盖构造、后续赋值和 NBT 加载，不修改传入的源堆叠；现有护甲投影同样受这层保护。

锁定期间转发手杖自身支持的工具能力与模块附魔；第三方依赖原物品 ID、原始组件或对公开堆叠直接写入数据的强化流程需要单独适配。任意模组绕过入口自行创造其他物品仍不是这里的通用隔离承诺。现有护甲投影的组件镜像结构没有在本次改成工具投影结构。

## 消耗和附魔

原生每个耐久消耗点折算为 **1,000 FE**。挖掘与攻击各消耗一个点，剪切、砧板等沿原生成功操作回调扣费一次。熔炼和收集不重复收费。原生不扣耐久的扳手操作不额外收费，但需要至少 1,000 FE 才可操作。

电量不足时阻止挖掘、攻击、剪切等效果。手杖不会因电量耗尽损坏，模块和设置保留；仍可打开配置。耐久附魔不减少 FE 消耗；经验修补不充电，也不会因手杖耗电而消耗经验。

本体拒绝直接附魔。增强模块通过铁砧附魔并保存真实 `ItemStack`。拆卸返回原附魔模块。采集增强接受按实际工具定义声明的附魔，明确排除效率和直接精准采集；伤害增强接受原生剑类战斗附魔及有刀具模块时的农夫乐事背刺。

时运模式使用 `max(3, 模块有效时运等级)`；精准模式为精准采集 I。普通模式仍保留其他适用工具附魔。互斥附魔阻止安装和模式切换，外部编辑或数据包重载造成冲突时不会同时运行互斥效果。

速度模块在方块与玩家效果计算完原始进度后应用一次 `P / (B × P + K)`，保留零进度和自定义方块限制。B 为 0、3、5、10、20 tick；K 为 0.01、0.05、0.1、0.2、0.5、1。

## 兼容边界

产物流程是原生战利品／剪切／砧板配方 → 自动熔炼一次 → 插入背包 → 余量落地。方块掉落处理在事件最终取消判定之后执行。没有周围物品扫描、ME 路由或额外熔炉经验。

农夫乐事砧板保留原生配方选择、输入消耗、时运计算与工具扣费。砧板以外的食物切片和生物击杀产物未接入熔炼／收集流程。

Mek 操作调用其原生配置器与方块扳手入口，保留侧面配置、机器数据与安全检查。神化自然祝福在催熟前检查完整等级费用，然后由原生扣费回调消耗 FE。

第三方附魔按其工具定义接收；实测的兼容版本见下。没有对所有模组中绕开标准工具、附魔或消耗入口的实现作通用兼容承诺。

## 验证记录（2026-09-08）

- Java 21，Minecraft 1.21.1，NeoForge 21.1.220：`build` 成功，**914 项单元测试通过**。
- **38 项 GameTest 通过**：同时工具能力、采集等级、自定义负硬度方块、挖掘进度、原生掉落、附魔持久化与互斥、铁砧、背包溢出与取消、剪切／南瓜／蜂巢、FE 耗费与空电拦截、工作台安装拆卸、装备配置，以及下面三个真实模组交互；另覆盖全部伤害档位、五种范围、原生战斗附魔、取消、蓄力、视线／盟友／宠物、逐目标 FE、处决死亡／移除与存档迁移；闪电专项覆盖真实工作台绑定、网络库存扣费、16:1 补偿、资源不足拦截、未命中退款、部分提取回滚和范围闪电预算。
- 实测 Mekanism **10.7.19**、Farmer's Delight **1.3.4**、Apothic Enchanting **1.6.2**（Placebo 9.9.2，Apothic Attributes 2.9.1）。
- 移除上述可选模组及 Mek 附属后的启动与基础 GameTest 通过；此配置下三个对应的模组专项用例跳过。
- 隔离客户端实测原有装备配置页的手杖标签、十二个模块同步、开关、参数修改、副手识别及 7×7／无限伤害／处决模式。截图位于 `run-staff-client/screenshots/mimicry-staff-{hub,combat,execution}-zh.png`。
- 相位锁定相关共 14 项 GameTest：两种提取模式及嵌套载荷、界面交换与解锁、原生丢弃与重生、掉落实体构造／赋值／NBT，以及三个真实模组路径；另覆盖私有槽单次解锁、失效与错误位置清除、代次补位、持久化、原版临时战利品副本、投影战斗的真实闪电／FE／处决记录，以及投影在真实砧板和 Mek 配置／拆卸中的能力转发。NeoForge 21.1.248 上实测 PneumaticCraft 8.2.23、Reliquary 2.0.80.1570、Chance Cubes 5.0.2.517；同时带 Farmer's Delight 1.3.4、Apothic Enchanting 1.6.2、Apothic Attributes 2.10.1。正常构建仍为 NeoForge 21.1.220。
- GameTest 和客户端探针均在 `src/jdb`，不进入发布 JAR。

常规构建：`./gradlew build`。服务端探针：`./gradlew runStaffGameTestServer`。指定真实可选模组时用 `-Pae2ltJdbProbeMods`，值为平台路径分隔符连接的 JAR 路径。

客户端探针 `runStaffClient` 仅用于专门的测试存档 `run-staff-client/saves/StaffPreview`，会重置该测试玩家物品并生成测试台面。它验证 UI 数据包和截图，不应指向正常游戏存档。
